export interface blogCard {
  title: string;
  image: any;
  body: string;
  postedAt: Date;
  currentSlug: string;
}
export interface fullBlog {
  title: string;
  image: any;
  body: string;
  postedAt: Date;
  currentSlug: string;
}
